# 預覽修正追蹤

- [x] 將目前的預覽主機加入 Vite 開發伺服器白名單。
- [x] 重新啟動開發伺服器並確認預覽頁面正常載入。
- [ ] 保存可用的修正版本並通知使用者。

# 留白作品集風格重構

- [x] 以新參考圖為唯一視覺規格，重做首頁與履歷閱讀版面。
- [x] 將使用者提供的頭像移至靜態素材區並用於首頁。
- [x] 建立三張重疊卡片點擊後展開同一組五張作品卡的互動骨架。
- [x] 驗證桌面與手機版的互動、捲動與預覽載入狀態。

## 預覽驗證備註

已確認預覽首頁可載入使用者提供的圓形頭像，首頁的「點開作品集」控制可開啟標題為 `SELECTED WORKS / 05` 的全螢幕瀏覽層，並顯示同一組五張可點擊的作品占位卡。展開模式中發現卡片的柔和色彩被預設背景覆蓋，已修正 CSS 選擇器並重新確認五張卡均正確顯示不同色調。選取第三張卡後，該卡會聚焦於中央，其他卡會柔焦，並顯示對應的短簡介與作品連結占位資訊。關閉控制可回到首頁的三張疊放卡狀態；底部膠囊導覽的經歷按鈕也已驗證可正確定位至工作經驗區段。

最終驗證已執行 `pnpm install --frozen-lockfile && pnpm run build`，Vue 型別檢查與 Vite 生產建置皆成功完成。

# 作品集互動細節優化

- [x] 回滾誤觸的 GitHub 頭像替換，恢復使用者提供的頭像。
- [x] 重製主頁視窗下緣的漸隱／柔焦效果，並修正五張作品卡的陰影過渡。
- [x] 讓卡片聚焦後仍可直接點擊其他作品卡進行切換。
- [x] 提升展開、聚焦與關閉動畫的連續性，並檢查按鈕可點擊層級。
- [x] 驗證手機與桌面版互動，保存最終版本。

已確認主頁下緣在底部導覽上方有漸進的 Cloud White 覆蓋與淡化效果，保持底部膠囊導覽清晰可點。作品集展開層的五張卡均可觸及，且右上方關閉按鈕位於獨立的最高互動層，不受卡片或下方浮動元素遮擋。第三張作品卡聚焦後，已直接點選第二張卡並確認焦點與短簡介同步切換，無須退出回到首頁。

手機版已確認主頁下緣保有由清晰至輕微失焦的漸進層次，底部導覽仍清楚可點；最終 `pnpm run build` 已成功通過 Vue 型別與 Vite 生產建置。

## 精確規格驗證備註

預覽首頁已確認頂部小標包含生日 `1999.10.10` 與 `INFJ`，首頁不再顯示「嗨，我是 Nana。」與「關於我」標頭。作品集展開層目前五張卡只保留圖片視覺，不顯示卡片內索引、圓框或「作品待設定」文字；展開層仍保留獨立的右上關閉按鈕與底部關閉控制。

  

本輪互動測試確認選中作品會將簡介與開頭連結 icon 放在圖片下方，五張卡仍維持純圖片。背景點擊測試的座標仍可能落在展開層的內容區，選中狀態未穩定清除，下一步需把「背景關閉目前小卡」改成明確的獨立 backdrop hit-area，避免與作品卡舞台重疊。

  

遮罩互動已改為明確的全螢幕背景 hit-area，作品卡與背景不再共用穿透事件；選中作品後，背景只會清除目前選中狀態，作品圖片本身仍可直接操作。右上關閉按鈕與底部關閉控制均維持獨立層級。

  

遮罩測試紀錄：新加入的背景 hit-area 已出現在可存取元素清單，但點選舞台背景後選中簡介仍存在，表示目前事件層級或點擊座標仍需進一步修正；在此修正完成前，不視為背景關閉行為通過。

  

背景 click 最終驗證：直接觸發舞台背景事件並等待 Vue Transition 更新後，`.project-note` 已消失，五張作品集展開層仍保留；因此背景只會關閉目前選中小卡，不會穿透選取其他卡，也不會直接關閉整個作品集。

  

重新選取第三張作品後，確認五張卡仍只呈現圖片，選中卡的簡介與開頭連結 icon 位於圖片下方，其他卡保持可操作的模糊狀態；下一步以明確背景 hit-area 完成最後清除測試。

  

實際背景按鈕測試補充：以瀏覽器元素點擊 `collection-stage__backdrop` 後，等待轉場仍回傳 `cards: 5`、`overlay: true`、`note: true`；事件層目前仍未在真實操作路徑清除選中狀態，需再檢查元素命中與 handleBackdropClick 綁定。

  

事件層診斷：展開層背景按鈕尺寸為 `1024×656`，舞台位於 `x=121, y=80`，選中簡介位於舞台下方。可存取元素點擊會使用背景按鈕中心點，而該點可能落在選中作品圖片上；因此後續改用舞台內不被卡片或簡介覆蓋的空白座標驗證背景關閉，避免把命中作品卡誤判成背景事件失效。

  

最新預覽畫面確認：五張作品卡已移除圓框、索引與卡內文字，選中卡中央放大後只在圖片下方顯示 `↗` 與簡介文字；未選中卡維持柔焦但仍保留直接點擊切換能力。右上關閉按鈕保持在最高層，底部關閉控制仍可用。

  

# 精確規格調整（本輪）

- [x] 將首頁與展開層作品卡改為純圖片，移除圓框與卡片內文字。
- [x] 將選中作品的簡介放在圖片下方，並於文字開頭加入連結 icon。
- [x] 阻止背景模糊層穿透點擊其他作品，只允許點擊背景關閉當前小卡。
- [x] 移除首頁開場問候與「關於我」大標題，調整內容換行與位置。
- [x] 在頂部小標加入生日與 INFJ，移除不適用的求職文案並逐行排列連結。
- [x] 檢查同列文字、標籤與 icon 的垂直置中及桌面／手機版對齊。
- [x] 保存更新版本並交付。

本輪已完成 `pnpm run build`、桌面首頁截圖與手機首頁截圖；預覽互動確認五張卡可直接切換，選中簡介位於圖片下方，背景 hit-area 只清除選中狀態，明確關閉按鈕才會離開作品集。

  

# 純 Vue 專案清理

- [x] 盤點並移除 React／React DOM／React plugin／React 元件檔案與引用。
- [x] 確認 Vite、TypeScript、Vue 入口與建置設定完全一致。
- [x] 執行 Vue 型別檢查、正式建置與預覽驗證。
- [ ] 保存純 Vue 修正版本並交付檢查結果。

  

# 作品卡動畫與互動隔離

- [x] 確認現有作品卡的位置、選中狀態與簡介結構。
- [x] 讓卡片從原位置平滑移動到中央並回正，降低閃爍感。
- [x] 將簡介與圖片拆成完全獨立、上下不重疊的區塊，保留少量白邊。
- [x] 阻止選中卡狀態下模糊背景穿透觸發其他作品卡。
- [x] 驗證切換、關閉、Escape 與桌面／手機版互動並保存版本。

  

最新預覽展開層確認五張作品卡以直式圖片卡呈現，保留細白邊與圓角，未選中時排列清楚且沒有卡內文字。下一步測試選中卡由原位置移動到中央並回正，以及選中後其他卡的不可穿透狀態。

  

最新實測結果：第三張作品卡選中後會從原本右上位置移到舞台中央並回正，簡介獨立顯示於圖片下方；其他四張卡進入模糊且不可穿透狀態。點擊卡片／簡介以外的空白背景後，選中狀態會清除並回到五張作品瀏覽層，不會誤切換或離開展開層。

  

桌面與手機版檢查完成：首頁作品卡入口仍清楚可辨，底部導覽固定於視窗下方且未遮住入口；主頁內容接近視窗下緣時會逐步淡出並輕微失焦。作品集互動需以瀏覽器實際操作確認，不以靜態截圖判定動畫流暢度。

  

最新預覽互動確認：作品集展開層可正常開啟，五張卡維持直式純圖片、白邊與回正前的獨立排列；右上關閉按鈕和底部關閉控制仍在可操作位置。選中與背景隔離測試將以空白座標及鍵盤 Escape 進行，不使用背景中心點作為命中依據。

  

最終背景隔離實測：選中第三張卡後，瀏覽器可存取元素只保留目前選中卡，其餘模糊卡已被 `pointer-events: none` 隔離；點擊作品卡與簡介以外的空白背景後，簡介消失、五卡瀏覽層保留，符合「背景只關閉當前小卡」的 UX。

  

# 學歷與證照分段排版

- [x] 確認學歷與證照目前元件及資料欄位。
- [x] 讓時間獨立一行，標題與補充資訊分行呈現。
- [x] 參照工作經歷的間距與字級，保持桌面／手機版對齊。
- [x] 執行建置與預覽驗證，保存更新版本。

  

# 純靜態化整理

- [x] 盤點伺服器啟動、Express 相容層、後端檔案與不必要依賴。
- [x] 移除伺服器程式與後端相容設定，改以 Vite 直接輸出靜態檔。
- [x] 確認履歷文字與圖片均可由前端資料與可部署資源直接使用。
- [x] 驗證 GitHub Pages Actions 建置、預覽與資源載入，保存更新版本。

  

# 履歷定位與導覽更新

- [x] 將 document title 改為 `Nana｜Resume` 等通用寫法。
- [x] 以使用者提供的文字替換首屏自介，移除前端定位誤導。
- [x] 將 Web Design 內容合併至 Front End。
- [x] 加入 Experience、Education、Skills、Certification 標題。
- [x] 將底部導覽改為 Top、Experience、Education、Skills、Bottom。
- [x] 為證書與聯絡連結加入一致的線性 icon。
- [x] 驗證內容、對齊、連結與響應式版面，保存版本。

  

本輪驗證結果：桌面與手機版均顯示通用 Title `Nana｜Resume`、新的全端方向自介與生日／INFJ 小標；Web Design 已併入 Front End。底部導覽已改為 Top、Experience、Education、Skills、Bottom，證書與聯絡連結使用一致的 Lucide 線性 icon，長文字與 icon 在窄螢幕仍保持可讀與垂直對齊。`pnpm run build` 已成功完成。

  

# 舊 About 內容與英文區段標題修正

- [x] 移除舊 about 區塊中的重複自介與「把需求釐清，再把細節交付。」。
- [x] 將 Experience、Education、Skills、Certification 做成畫面上明確可見的副標題。
- [x] 驗證頁面不再出現舊文案，並確認桌面／手機版標題對齊。
- [x] 保存更新版本並交付。

  

本輪驗證結果：桌面與手機完整頁面均已移除「把需求釐清，再把細節交付。」及舊版 about 自介；Experience、Education、Skills、Certification 現在以真正的區段主標題顯示，並各自保留中文輔助標籤。建置與兩種 RWD 預覽均正常。

# 作品集互動與段落標題再修正

- [x] 放大作品集展開畫面中的小卡尺寸，並確認手機版不溢出。
- [x] 展開所有小卡時只讓小卡顯示 pointer，背景不可點擊提示且鎖定整頁滾動。
- [x] 選取單張小卡時只讓當前小卡顯示 pointer，移除簡介前箭頭，改用卡片 hover 暗色遮罩與中央 link icon。
- [x] 讓 Experience、Education、Skills、Certification 段落標題在實際頁面上清楚可見且具足夠層級。
- [x] 完成桌面／手機互動與建置驗證，保存並交付更新版本。

  


# 首頁文案、卡片版面與部署浮動按鈕調整

- [x] 將首屏自介替換為使用者提供的三段完整文案。
- [x] 將 meta description 改為「林家淇 Nana／0nn，全端工程師履歷。」。
- [x] 將頭像移到灰色 Nana／0nn 資訊列正上方，縮短垂直距離。
- [x] 將首頁三張作品卡整體左移，並確認手機版不超出可視範圍。
- [x] 放大展開作品卡、縮短圖片與簡介距離，提升簡介文字對比。
- [x] 檢查並調整部署後 Made with Manus 按鈕至頁面上方中央，為內容預留安全空間。
- [x] 完成卡片比例量測、桌面／手機驗證、正式建置與 checkpoint 保存。


# 導覽列與首屏對齊調整

- [x] 將底部 capsule 導覽列改為頁面上方定位，並保留平台徽章的上方安全空間。
- [x] 確認首屏新版自介維持使用者指定的段落分行與透氣間距。
- [x] 讓頭貼底部與右側首頁作品卡的可視底部對齊，並兼顧手機版比例。
- [x] 完成桌面／手機截圖、正式建置與 checkpoint 保存。


# 上緣柔焦與 2:3 作品卡比例調整

- [x] 移除目前為平台徽章預留的額外上方安全留白，保留正常上方導覽列空間。
- [x] 在頁面上緣加入與下緣一致的邊界柔焦效果，且不遮擋導覽與內容。
- [x] 將首頁預覽卡與展開作品卡統一改為 2:3 比例，並修正桌面／手機間距。
- [x] 完成桌面／手機截圖、正式建置與 checkpoint 保存。


# 上緣柔焦、自介空行與 GitHub Pages 檢查

- [x] 加深頁面上緣的邊界柔焦，維持導覽列與首屏內容清楚可見。
- [x] 將自介保留為四個段落，段落之間使用使用者指定的空行間隔。
- [x] 檢查 https://github.com/0nn7a/resume-by-manus.git 是否可存取，並確認 GitHub Actions／Pages 部署條件。
- [x] 完成建置與預覽驗證，保存可延續 GitHub Pages 流程的版本。


# 自介明確換行分隔調整

- [x] 將四段自介改為明確的換行分隔效果，避免只依賴預設段落 margin。
- [x] 保留使用者指定的四段文字與空行節奏，並確認桌面／手機版一致。
- [x] 完成正式建置與預覽驗證，保存更新版本。


# Experience 內文與中文字重調整

- [x] 將前端開發、系統開發與維運、自主進修改為使用者指定的精簡條目。
- [x] 下修中文標題、職稱與內文的粗體字重，維持層級但避免過厚。
- [x] 檢查同步後 GitHub repository 的存取權限、分支與檔案狀態。
- [x] 完成排版、正式建置與預覽驗證，保存更新版本。


# GitHub 首次推送與 Pages 部署

- [x] 將細節內文中的全形／統一改為半形 `/`。
- [x] 確認本地 main、GitHub Actions workflow 與遠端 repository 狀態。
- [x] 依使用者授權首次 push 到 `0nn7a/resume-by-manus` 的 main 分支。
- [x] 檢查 Actions 執行結果與 GitHub Pages 實際部署網址（Pages 尚未啟用，尚未產生網址）。
- [x] 保存同步版本並回報部署狀態。


# 斜線空格與公開 Pages 重新檢查

- [x] 盤點所有可見半形 `/`，確保前後各有且只有一個空格。
- [x] 確認 repository 已為 Public、Pages source 為 GitHub Actions，並重新推送修正。
- [x] 檢查最新 Actions 執行結果、Pages 實際網址與首頁資產載入。
- [x] 完成正式建置與預覽驗證，保存更新版本。


# Mobile Safari 柔焦邊界修正

- [x] 檢查上下柔焦偽元素目前使用的固定高度與 viewport 單位。
- [x] 以 `100dvh`、`env(safe-area-inset-*)` 與固定定位修正底部貼齊。
- [x] 驗證手機 viewport、作品集展開狀態與桌面版不受影響。
- [x] 完成正式建置並保存更新版本。


# Mobile Safari 柔焦二次修正

- [x] 重新檢查公開版本與本地版本的柔焦幾何，確認問題是否來自固定偽元素與 Safari visual viewport 差異。
- [x] 改用更直接的固定 viewport 柔焦層，避免安全區計算造成白邊停在工具列上方。
- [x] 重新建置、推送並驗證 GitHub Pages 與手機 viewport。
- [x] 保存修正版並說明仍需實機確認的 Safari 限制。


# 導覽列回到底部、Safari 柔焦與 Manus favicon

- [x] 將導覽列從上方移回頁面下方，並保留手機底部安全區。
- [x] 重新檢查 Safari 柔焦問題，避免只用 visualViewport 高度猜測固定邊界。
- [x] 尋找並採用可公開載入的 Manus 官方 favicon／logo 資產。
- [x] 完成手機／桌面預覽、正式建置、GitHub Pages 部署與版本保存。


# 作品資料與卡片層級調整

- [x] 將下緣柔焦加深至接近上緣，並保留底部導覽列清楚可見。
- [x] 依使用者提供的順序填入五張作品圖片、作品名稱與作品連結。
- [x] 展開所有作品卡時，讓作品一位於最前層，後續作品依重要程度排列 z-index。
- [x] 驗證圖片載入、卡片選取／外部連結與桌面／手機版顯示。
- [x] 完成正式建置、推送 GitHub Pages 並保存版本。

# 聯絡區與作品展示再優化

- [x] 增加聯絡資訊區與證照區之間的垂直間距。
- [x] 加入 LinkedIn 聯絡連結與一致的線性 icon。
- [x] 移除作品展示頁右上角叉叉，只保留下方膠囊風格關閉按鈕。
- [x] 調整展開作品卡散落位置，避免遮擋下方關閉按鈕。
- [x] 降低作品卡圓角並移除示意底色。
- [x] 放大置中的選取作品卡，強化簡介白底容器與 hover 遮罩／連結 icon 對比。
- [x] 執行 RWD、互動、正式建置與 GitHub Pages 部署驗證。

# 作品展示構圖與選取狀態再調整

- [x] 讓未選取的五張卡片維持較小尺寸並自然分布，不壓到展示標題。
- [x] 僅在卡片被選取並移至中央後放大，避免散落狀態擁擠。
- [x] 讓作品簡介白底容器依文字內容收窄，不佔過寬畫面。
- [x] 驗證桌面／手機展示構圖、選取聚焦與重新部署。

# 撲克牌式作品展示構圖再調整

- [x] 將未選取的五張卡片改為上排三張、下排兩張的清楚構圖。
- [x] 保留輕微旋轉與錯位，避免卡片看起來像亂飛。
- [x] 進一步放大置中的選取卡片，充分使用展示空間。
- [x] 驗證桌面／手機版構圖、選取聚焦與底部 X 關閉流程。

# 作品展示層跑版修復

- [x] 檢查展開層實際高度、卡片放大後的邊界與關閉按鈕可點範圍。
- [x] 縮短展開層空白，讓後續履歷內容不被不必要地推到畫面外。
- [x] 將關閉按鈕固定於清楚且容易操作的位置，避免被卡片或畫面邊緣遮擋。
- [x] 保留中央選取卡放大，同時修正桌面與手機版的排版。
- [x] 完成建置、瀏覽器互動與公開部署驗證。
