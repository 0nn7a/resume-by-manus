/* Design system: the Vue entry point keeps all presentation inside an editorial, print-ready single-page application. */
import { createApp } from "vue";
import App from "./App.vue";
import "./styles/resume.css";

createApp(App).mount("#app");
